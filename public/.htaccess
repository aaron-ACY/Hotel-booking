RewriteEngine On

# Kiểm tra nếu yêu cầu không phải là thư mục thực tế
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Chuyển hướng tất cả về file index.php
RewriteRule ^(.*)$ index.php?url=$1 [L,QSA]
